name: Defensible Vertical Slice
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    uses: MelodicBloom/.github/.github/workflows/reusable-defensible-vertical-slice.yml@d6b1d5a950a2d0166e174f7ad3560dfbf0f54abb
    with:
      # package-lock.json was regenerated and frozen-install validated on 2026-08-01.
      node-version: "20"
      package-manager: npm
      profile-path: .melodicbloom/verification-profile.json
      lint-command: npm run lint
      typecheck-command: npm run type-check
      build-command: npm run build
      specialized-command: npm run format:check
      artifact-path: .next
