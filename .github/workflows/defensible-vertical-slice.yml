name: Defensible Vertical Slice
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    uses: MelodicBloom/.github/.github/workflows/reusable-defensible-vertical-slice.yml@51d2de7b08a91cadc59a82899b25a46d78151fa2
    with:
      # package-lock.json was regenerated and frozen-install validated on 2026-08-01.
      node-version: '20'
      package-manager: npm
      profile-path: .melodicbloom/verification-profile.json
      lint-command: npm run lint
      typecheck-command: npm run type-check
      build-command: npm run build
      specialized-command: npm run format:check
      artifact-path: .next
