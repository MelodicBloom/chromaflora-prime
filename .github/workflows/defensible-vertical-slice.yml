name: Defensible Vertical Slice
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    uses: MelodicBloom/.github/.github/workflows/reusable-defensible-vertical-slice.yml@architecture/defensible-vertical-slice-v1
    with:
      node-version: "20"
      package-manager: npm
      profile-path: .melodicbloom/verification-profile.json
      lint-command: npm run lint
      typecheck-command: npm run type-check
      build-command: npm run build
      specialized-command: npm run format:check
      artifact-path: .next
